# Fichier .env à la racine du projet (C:/ComplaintAppV1/.env)

# -- URL DE MIGRATION (utilisée par Prisma CLI depuis votre machine locale) --
# IMPORTANT: Doit pointer vers localhost car la commande prisma est exécutée depuis votre machine
DATABASE_URL="sqlserver://sa:%40Ega1234@localhost:1433;database=ComplaintDev;trustServerCertificate=true;"

# -- URL POUR L'API LOOKUP (lecture seule) - utilisée dans Docker --
# Le mot de passe 'Ega1234' ne contient pas de caractères spéciaux, il reste donc inchangé.
LOOKUP_DATABASE_URL="sqlserver://mobile_lookup:Ega1234@db:1433;database=ComplaintDev;trustServerCertificate=true;"

# -- URL POUR L'API TRANSACTIONNELLE (écriture) - utilisée dans Docker --
# Le mot de passe 'Ega1234' ne contient pas de caractères spéciaux, il reste donc inchangé.
TRX_DATABASE_URL="sqlserver://mobile_service:Ega1234@db:1433;database=ComplaintDev;trustServerCertificate=true;"

# -- Configuration des serveurs --
LOOKUP_API_PORT=3001
TRX_API_PORT=3000

# -- Configuration de l'antivirus --
CLAMAV_HOST=clamav
CLAMAV_PORT=3310