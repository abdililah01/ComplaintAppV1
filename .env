# ────────────────────────────────────────────────────────────────────
# FILE : COMPALINTAPPV1/.env
# COMPALINTAPPV1/.env  (at repo root – used by docker-compose & CI)
# ────────────────────────────────────────────────────────────────────

# --- DB secrets (for the SQL container) ---
DB_SA_PASSWORD=@Ega1234
DB_LOOKUP_USER=mobile_lookup
DB_LOOKUP_PASSWORD=@Ega1234
DB_SERVICE_USER=mobile_service
DB_SERVICE_PASSWORD=@Ega12345678

# --- Connection string (optional – usually overridden per‐service) ---
# You can omit this if you set DATABASE_URL explicitly in docker-compose.
#DATABASE_URL="sqlserver://db:1433;database=ComplaintDev;user=${DB_SERVICE_USER};password=${DB_SERVICE_PASSWORD};trustServerCertificate=true;encrypt=false"

# --- API ports (container) ---
LOOKUP_API_PORT=3001
TRX_API_PORT=3000

# --- ClamAV (container hostnames) ---
CLAMAV_HOST=clamav
CLAMAV_PORT=3310
